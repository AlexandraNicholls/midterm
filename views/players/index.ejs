<!DOCTYPE HTML>
<html>
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <main>
            <h1><%= title %></h1>
            <!-- list the players -->
            <ul class="list-group">
            <% for (var i = 0; i < players.length; i++) { %>
                <li class="list-group-item">
                    <a href="/players/<%= players[i]._id %>" title="Player">
                        <%= players[i].title %>
                    </a>
                    <br />
                    <%= players[i].created.toDateString() %>
                </li>
            <% } %>
            </ul>
        </main>
    </body>
</html>